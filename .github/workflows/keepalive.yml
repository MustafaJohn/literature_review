name: Keep Render Service Alive

on:
  schedule:
    - cron: "*/3 * * * *"   # every 3 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render service
        run: curl -s https://research-assistant-rp8i.onrender.com/api/health
